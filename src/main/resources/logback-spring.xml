<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Ruta de logs -->
    <property name="LOG_PATH" value="${LOG_PATH:-./logs}" />

    <!-- ==================== PATTERNS DEV ==================== -->

    <!-- Logger completo -->
    <property name="DEV_PATTERN"
              value="%d{HH:mm:ss.SSS} %highlight(%-5level) %cyan(%logger) %method [%file:%line] - %msg%n" />

    <!-- Alternativa más detallada con hilo -->
    <!--
    <property name="DEV_PATTERN"
              value="%d{HH:mm:ss.SSS} [%thread] %highlight(%-5level) %cyan(%logger) %method [%file:%line] - %msg%n" />
    -->

    <!-- ==================== PERFIL DEV / DEFAULT ==================== -->
    <springProfile name="default,dev">

        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
                <pattern>${DEV_PATTERN}</pattern>
                <charset>UTF-8</charset>
            </encoder>
        </appender>

        <!-- Tus logs de la app -->
        <logger name="com.lorenzomar3.AQ" level="DEBUG" />

        <!-- SQL de Hibernate, descomentá si querés ver consultas -->
        <!--
        <logger name="org.hibernate.SQL" level="DEBUG" />
        <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE" />
        -->

        <!-- Spring en un nivel razonable -->
        <logger name="org.springframework" level="INFO" />

        <root level="INFO">
            <appender-ref ref="CONSOLE" />
        </root>

    </springProfile>

    <!-- ==================== PERFIL PROD ==================== -->
    <springProfile name="prod">

        <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeCallerData>true</includeCallerData>
                <fieldNames>
                    <timestamp>timestamp</timestamp>
                    <level>level</level>
                    <thread>thread</thread>
                    <logger>logger</logger>
                    <message>message</message>
                </fieldNames>
            </encoder>
        </appender>

        <appender name="JSON_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_PATH}/application.log</file>

            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeCallerData>true</includeCallerData>
                <fieldNames>
                    <timestamp>timestamp</timestamp>
                    <level>level</level>
                    <thread>thread</thread>
                    <logger>logger</logger>
                    <message>message</message>
                </fieldNames>
            </encoder>

            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>${LOG_PATH}/application.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
                <maxFileSize>10MB</maxFileSize>
                <maxHistory>30</maxHistory>
                <totalSizeCap>500MB</totalSizeCap>
            </rollingPolicy>
        </appender>

        <logger name="com.lorenzomar3.AQ" level="INFO" />
        <logger name="org.springframework" level="WARN" />

        <root level="WARN">
            <appender-ref ref="JSON_CONSOLE" />
            <appender-ref ref="JSON_FILE" />
        </root>

    </springProfile>

</configuration>